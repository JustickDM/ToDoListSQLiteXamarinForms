<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 x:Class="ToDoList.Controls.MasterDetail.Views.ToDoListPage"
			 Title="ToDoList Page">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add"
                     Clicked="ToolbarItem_Clicked">
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="10"
                     Padding="20">

            <ListView x:Name="List" 
                  ItemsSource="{Binding Notes}"
                  ItemSelected="List_ItemSelected"
                  SelectedItem="{Binding SelectedNote, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding NoteId}"/>
                                <Label Text="{Binding NoteText}"/>
                                <Label Text="{Binding NoteDateTimeStart}"/>
                                <Label Text="{Binding NoteDateTimeFinish}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
